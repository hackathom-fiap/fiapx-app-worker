spring:
  rabbitmq:
    host: ${RABBIT_HOST}
    port: ${RABBIT_PORT}
    username: ${RABBIT_USER}
    password: ${RABBIT_PASSWORD}
    ssl:
      enabled: true

video:
  api:
    url: ${VIDEO_API_URL:http://localhost:8082/api/videos}
